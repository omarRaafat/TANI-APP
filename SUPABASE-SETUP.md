# ربط تاني بـ Supabase

## الخطوات

**١ — اعمل مشروع**
من [supabase.com](https://supabase.com) اعمل مشروع جديد، واختار المنطقة `eu-central` أو `me-central` (أقرب للسوق المصري والسعودي).

**٢ — شغّل السكيما**
افتح **SQL Editor** والصق محتوى `supabase-setup.sql` كله، ودوس Run. ده بيعمل:
- جدول `listings` بقيود على كل عمود
- سياسات RLS: أي حد يقرا، والبايع بس يعدّل ويمسح إعلانه
- تريجر يمنع أكتر من ٢٥ إعلان منشور للمستخدم الواحد
- bucket للصور اسمه `listing-photos` بحد ٣ ميجا للصورة
- تفعيل البث الحي على الجدول

**٣ — فعّل طرق الدخول**

| المزود | المكان | ليه |
|---|---|---|
| Anonymous | `Authentication → Sign In / Providers` | عشان الزائر يتصفح قبل ما يوثّق |
| Email | نفس المكان | قناة OTP احتياطية — مجانية وشغالة فوراً |
| Phone | نفس المكان | قناة OTP الأساسية — محتاجة مزوّد SMS |

**مهم:** Supabase مش بيبعت SMS بنفسه. لازم تربط مزوّد من جوه لوحة التحكم:

| المزود | مناسب لـ | ملاحظات |
|---|---|---|
| **Unifonic** | السعودية | الأشهر في الخليج، مدعوم عبر إعداد مخصص |
| **Twilio Verify** | الاتنين | أسهل ربط، بس أغلى للرسالة |
| **MessageBird** | الاتنين | أرخص من Twilio غالباً |
| **Vonage** | الاتنين | بديل معقول |

**في مصر تحديداً:** لازم تسجّل الـ Sender ID عند المشغّلين (فودافون/أورنج/اتصالات) قبل ما الرسايل تعدّي، والعملية بتاخد أيام. لحد ما تخلص، سيب قناة الإيميل شغالة كبديل.

**للتجربة دلوقتي حالاً:** فعّل الإيميل بس. بيشتغل من غير أي مزوّد ولا فلوس، والـ OTP بيوصل على طول.

**٤ — حط المفاتيح**
من `Project Settings → API` انسخ الـ URL والـ `anon` key، وحطهم في أول `tani-supabase.js`:

```js
var CONFIG = {
  url:     'https://xxxxx.supabase.co',
  anonKey: 'eyJhbGciOi...'
};
```

**٥ — ارفع الملفات**
الأربع ملفات لازم يبقوا في نفس الفولدر:

```
index.html
cart.html
checkout.html
tani-supabase.js
```

---

## إزاي بيشتغل

**الصور** بتتصغّر في المتصفح لـ ١٤٠٠ بكسل وتتضغط JPEG قبل الرفع، وبتروح Storage كملفات. الجدول بيخزن الروابط بس — مفيش base64 خالص، فمشكلة الـ ٥ ميجا اتحلّت.

**كل مستخدم بياخد `auth.uid()`** من الدخول المجهول، وبيترفعله الصور في فولدر باسمه. سياسة Storage بتمنعه يرفع في فولدر حد تاني، وسياسة الجدول بتمنعه يمسح إعلان مش بتاعه — ده متحقق منه على السيرفر مش في المتصفح.

**البث الحي** شغال: لو حد نشر إعلان وإنت فاتح الموقع، هيظهر عندك من غير رفريش.

**السلة والمفضلة** فاضلين محليين — مالهمش لازمة على السيرفر قبل ما يبقى فيه حسابات حقيقية.

**لو Supabase وقع أو مش متظبط**، الطبقة بترجع تلقائياً لـ localStorage والموقع يفضل شغال. هتلاقي رسالة في الـ console توضح.

---

## التوثيق بـ OTP

**الفكرة:** الزائر بيدخل مجهول ويتصفح عادي. أول ما يحاول ينشر إعلان، بيتفتحله نافذة توثيق.

**الحتة المهمة:** الكود بيستخدم `updateUser()` مش `signInWithOtp()`. الفرق إن الأولى بتحوّل الحساب المجهول لحساب دائم **بنفس الـ uid**، فالإعلانات اللي نشرها قبل كده بتفضل مربوطة بيه. لو استخدمنا التانية كان هيتعمل مستخدم جديد وإعلاناته القديمة تتيتّم.

**التحقق على السيرفر:** فيه تريجر `enforce_verified_seller` بيرفض أي إدخال من مستخدم مش موثّق، وبيختم `seller_verified`. يعني حتى لو حد نادى الـ API مباشرة من غير المتصفح، مش هيعدي.

**تطبيع الأرقام** شغال للسوقين:

```
01066343688   → +201066343688
٠١٠٦٦٣٤٣٦٨٨   → +201066343688
0501234567    → +966501234567
٠٥٠١٢٣٤٥٦٧    → +966501234567
```

والأرقام الناقصة أو الغلط بترفض قبل ما نستهلك رسالة.

**شارة "موثّق"** بتظهر على كل إعلان من بايع موثّق، جوه الكارت وفي صفحة التفاصيل.

---

## حاجات لسه ناقصة قبل الإطلاق

**مفيش مراجعة فعلية للإعلانات** رغم إن الصفحة بتوعد بيها. أبسط تنفيذ: غيّر `default 'live'` لـ `default 'pending'` في الجدول، واعمل صفحة أدمن تحوّله لـ `live`.

**رقم تليفون البايع ظاهر لأي حد** بيقرا الجدول. لو ده يقلقك، اعمل view عامة من غير عمود التليفون، وخلي الرقم يتجاب بـ RPC بعد ما المشتري يعمل طلب فعلي.

**السلة والطلبات لسه محليين.** الطلب نفسه مبيتسجلش في قاعدة البيانات — محتاج جدول `orders` وربط صفحة الدفع بيه، وده الخطوة الطبيعية الجاية.

**حد إرسال الـ OTP** مضبوط من لوحة تحكم Supabase مش من الكود. اضبطه على ٥ رسايل للرقم في الساعة على الأكتر، وإلا حد ممكن يستهلك رصيدك.
