# ربط تاني بـ Supabase

## الخطوات

**١ — اعمل مشروع**
من [supabase.com](https://supabase.com) اعمل مشروع جديد، واختار المنطقة `eu-central` أو `me-central` (أقرب للسوق المصري والسعودي).

**٢ — شغّل السكيما**
افتح **SQL Editor** والصق محتوى `supabase-setup.sql` كله، ودوس Run. ده بيعمل:
- جدول `listings` بقيود على كل عمود
- سياسات RLS: أي حد يقرا، والبايع بس يعدّل ويمسح إعلانه
- تريجر يمنع أكتر من ٢٥ إعلان منشور للمستخدم الواحد
- bucket للصور اسمه `listing-photos` بحد ٣ ميجا للصورة
- تفعيل البث الحي على الجدول

**٣ — فعّل طرق الدخول**

| المزود | المكان | ليه |
|---|---|---|
| Anonymous | `Authentication → Sign In / Providers` | عشان الزائر يتصفح قبل ما يوثّق |
| Email | نفس المكان | قناة OTP احتياطية — مجانية وشغالة فوراً |
| Phone | نفس المكان | قناة OTP الأساسية — محتاجة مزوّد SMS |

**مهم:** Supabase مش بيبعت SMS بنفسه. لازم تربط مزوّد من جوه لوحة التحكم:

| المزود | مناسب لـ | ملاحظات |
|---|---|---|
| **Unifonic** | السعودية | الأشهر في الخليج، مدعوم عبر إعداد مخصص |
| **Twilio Verify** | الاتنين | أسهل ربط، بس أغلى للرسالة |
| **MessageBird** | الاتنين | أرخص من Twilio غالباً |
| **Vonage** | الاتنين | بديل معقول |

**في مصر تحديداً:** لازم تسجّل الـ Sender ID عند المشغّلين (فودافون/أورنج/اتصالات) قبل ما الرسايل تعدّي، والعملية بتاخد أيام. لحد ما تخلص، سيب قناة الإيميل شغالة كبديل.

**للتجربة دلوقتي حالاً:** فعّل الإيميل بس. بيشتغل من غير أي مزوّد ولا فلوس، والـ OTP بيوصل على طول.

---

### ربط Twilio للـ SMS — خطوة بخطوة

**أ) اعمل حساب واجمع ٣ بيانات**

من [console.twilio.com](https://console.twilio.com) بعد التسجيل:

| البيان | مكانه في Twilio |
|---|---|
| **Account SID** | الصفحة الرئيسية — بيبدأ بـ `AC` |
| **Auth Token** | نفس الصفحة — اضغط *Show* |
| **Message Service SID** | Messaging → Services — بيبدأ بـ `MG` |

> استخدم **Messaging Service** مش رقم واحد. ده بيخليك تضيف أرقام
> أو Sender IDs من غير ما تغيّر الإعداد في Supabase.

**ب) اعمل Messaging Service**

`Messaging → Services → Create Messaging Service`
- الاسم: `تاني OTP` (أو أي اسم)
- الاستخدام: **Verify users**
- في `Sender Pool` ضيف الرقم أو الـ Sender ID بتاعك

**ج) وصّلها بـ Supabase**

`Authentication → Sign In / Providers → Phone`

| الحقل | القيمة |
|---|---|
| Enable Phone provider | ✅ |
| SMS provider | **Twilio** |
| Twilio Account SID | `AC…` |
| Twilio Auth Token | التوكن |
| Twilio Message Service SID | `MG…` |

اضغط **Save**.

> ⚠️ متستخدمش **Twilio Verify** من القايمة — ده منتج تاني بواجهة
> مختلفة. اختار **Twilio** العادي، لإن الكود بيستخدم
> `verifyOtp({ type: 'sms' })` وده بيتوقع الإعداد العادي.

**د) اضبط حد الإرسال**

`Authentication → Rate Limits → SMS OTP`
المقترح: **٥ رسايل للرقم في الساعة** — بيمنع استنزاف رصيدك.

**هـ) جرّب**

الكود بيحوّل `01012345678` تلقائياً لـ `+201012345678`
(صيغة E.164 اللي Twilio بتطلبها) — مش محتاج تعمل حاجة.

في وضع التجربة المجاني، Twilio بتبعت **للأرقام الموثّقة بس**:
`Phone Numbers → Verified Caller IDs` → ضيف رقمك.

---

### ⚠️ مهم جداً قبل النشر في مصر

**لازم تسجّل Sender ID عند المشغّلين** (فودافون / أورنج / اتصالات / WE)
قبل ما الرسايل تعدّي للأرقام المصرية. العملية:

- بتاخد **أيام لأسابيع**
- بتحتاج **أوراق شركة** (سجل تجاري / بطاقة ضريبية)
- بتتعمل من خلال Twilio: `Messaging → Regulatory Compliance`

من غيرها الرسايل هتترفض أو متوصلش أصلاً، حتى لو الإعداد صح.

**خطة عملية:** ابدأ تسجيل الـ Sender ID دلوقتي لإنه بياخد وقت، وفي
نفس الوقت خلي **الإيميل** شغال كقناة أساسية (مجاني وفوري). لما الـ
Sender ID يخلص، الموبايل هيشتغل من غير أي تعديل في الكود.

**التكلفة التقريبية:** رسالة SMS لمصر بتكلف حوالي ‎$0.04–0.06‎ عند
Twilio. احسبها على عدد المستخدمين المتوقع — ١٠٠٠ توثيق ≈ ‎$50‎.

**٤ — ظبّط اسم المُرسِل وعنوان الإيميل**

الافتراضي بيوصل من `noreply@mail.app.supabase.io` وبعنوان إنجليزي زي
"Confirm Your Signup" — ده مش واضح للمستخدم المصري وبيروح Spam كتير.
غيّره من مكانين:

**أ) اسم المُرسِل** — `Authentication → Emails → SMTP Settings`

| الحقل | الحط |
|---|---|
| Sender name | `تاني` |
| Sender email | `no-reply@your-domain.com` |

> من غير SMTP خاص بيك، Supabase بتحد الإرسال بـ ٣ إيميلات في الساعة
> وبتتجاهل اسم المُرسِل. للنشر الحقيقي اربط SMTP (Resend أو Brevo —
> الاتنين عندهم باقة مجانية وبيشتغلوا في مصر).

> ⚠️ **حد الإرسال المجاني: ٣ إيميلات في الساعة.**
> ده أشهر سبب إن "الإيميل مش بيوصل" وانت بتجرّب. الخطأ اللي بيرجع
> هو `email rate limit exceeded` (status 429). لو وصلت للحد، استنى
> ساعة — أو اربط SMTP خاص بيك وده اللي بيشيل الحد خالص:
>
> **Resend** (٣٠٠٠ إيميل/شهر مجاني) أو **Brevo** (٣٠٠/يوم).
> بعد ما تعمل حساب، من `Authentication → Emails → SMTP Settings`
> فعّل **Enable Custom SMTP** وحط بيانات المزوّد.
>
> من غير SMTP خاص، الحد ده هيقف الموقع عن الشغل أول ما يزيد المستخدمين.

**ب) عنوان الرسالة ومحتواها** — `Authentication → Emails → Templates`

> ⚠️ **دي أهم خطوة.** القالب الافتراضي بيبعت **لينك للضغط**
> (`{{ .ConfirmationURL }}`)، بينما الموقع مستني **كود من ٦ أرقام**.
> لو سيبته زي ما هو، المستخدم هيوصله لينك ومش هيلاقي كود يكتبه —
> ولو ضغط اللينك مش هيتوثّق في الموقع.
>
> الحل: امسح `{{ .ConfirmationURL }}` وحط `{{ .Token }}` مكانه.

لازم تعدّل **قالبين**، لإن الكود بيستخدم الاتنين حسب الحالة
(`signInWithOtp` لحساب جديد، و`updateUser` لتحويل حساب مجهول):

| القالب | بيتبعت إمتى |
|---|---|
| **Magic Link** | أول مرة يوثّق بإيميل جديد |
| **Change Email Address** | لما يحوّل حساب مجهول لإيميل |

حط في **الاتنين** نفس الحاجة:

```
Subject: كود تأكيد حسابك في تاني — {{ .Token }}
```

وفي جسم الرسالة:

```html
<div dir="rtl" style="font-family:Tahoma,Arial,sans-serif;text-align:right;padding:24px">
  <h2 style="color:#0D3B31;margin:0 0 8px">كود التأكيد بتاعك</h2>
  <p style="color:#4B5159">ده الكود بتاع تأكيد حسابك في <b>تاني</b>:</p>
  <p style="font-size:32px;letter-spacing:10px;color:#BE4420;font-weight:bold;
            background:#FBFAF7;border:1px solid #E6E2D9;border-radius:8px;
            padding:16px;text-align:center;margin:16px 0">
    {{ .Token }}
  </p>
  <p style="color:#4B5159;font-size:14px">
    اكتب الكود ده في الموقع. صالح لمدة ساعة.
  </p>
  <p style="color:#7F868F;font-size:12px">
    لو مش إنت اللي طلبته، تجاهل الرسالة دي.
  </p>
</div>
```

**ليه الكود في العنوان؟** عشان المستخدم يشوفه من قائمة الإيميلات من غير
ما يفتح الرسالة — ده بيقلل الوقت والاحتكاك كتير.

**٥ — حط المفاتيح**
من `Project Settings → API` انسخ الـ URL والـ `anon` key، وحطهم في أول `tani-supabase.js`:

```js
var CONFIG = {
  url:     'https://xxxxx.supabase.co',
  anonKey: 'eyJhbGciOi...'
};
```

**٦ — ارفع الملفات**
الأربع ملفات لازم يبقوا في نفس الفولدر:

```
index.html
cart.html
checkout.html
tani-supabase.js
```

---

## إزاي بيشتغل

**الصور** بتتصغّر في المتصفح لـ ١٤٠٠ بكسل وتتضغط JPEG قبل الرفع، وبتروح Storage كملفات. الجدول بيخزن الروابط بس — مفيش base64 خالص، فمشكلة الـ ٥ ميجا اتحلّت.

**كل مستخدم بياخد `auth.uid()`** من الدخول المجهول، وبيترفعله الصور في فولدر باسمه. سياسة Storage بتمنعه يرفع في فولدر حد تاني، وسياسة الجدول بتمنعه يمسح إعلان مش بتاعه — ده متحقق منه على السيرفر مش في المتصفح.

**البث الحي** شغال: لو حد نشر إعلان وإنت فاتح الموقع، هيظهر عندك من غير رفريش.

**السلة والمفضلة** فاضلين محليين — مالهمش لازمة على السيرفر قبل ما يبقى فيه حسابات حقيقية.

**لو Supabase وقع أو مش متظبط**، الطبقة بترجع تلقائياً لـ localStorage والموقع يفضل شغال. هتلاقي رسالة في الـ console توضح.

---

## التوثيق بـ OTP

**الفكرة:** الزائر بيدخل مجهول ويتصفح عادي. أول ما يحاول ينشر إعلان، بيتفتحله نافذة توثيق.

**الحتة المهمة:** الكود بيستخدم `updateUser()` مش `signInWithOtp()`. الفرق إن الأولى بتحوّل الحساب المجهول لحساب دائم **بنفس الـ uid**، فالإعلانات اللي نشرها قبل كده بتفضل مربوطة بيه. لو استخدمنا التانية كان هيتعمل مستخدم جديد وإعلاناته القديمة تتيتّم.

**التحقق على السيرفر:** فيه تريجر `enforce_verified_seller` بيرفض أي إدخال من مستخدم مش موثّق، وبيختم `seller_verified`. يعني حتى لو حد نادى الـ API مباشرة من غير المتصفح، مش هيعدي.

**تطبيع الأرقام** شغال للسوقين:

```
01066343688   → +201066343688
٠١٠٦٦٣٤٣٦٨٨   → +201066343688
0501234567    → +966501234567
٠٥٠١٢٣٤٥٦٧    → +966501234567
```

والأرقام الناقصة أو الغلط بترفض قبل ما نستهلك رسالة.

**شارة "موثّق"** بتظهر على كل إعلان من بايع موثّق، جوه الكارت وفي صفحة التفاصيل.

---

## حاجات لسه ناقصة قبل الإطلاق

**مفيش مراجعة فعلية للإعلانات** رغم إن الصفحة بتوعد بيها. أبسط تنفيذ: غيّر `default 'live'` لـ `default 'pending'` في الجدول، واعمل صفحة أدمن تحوّله لـ `live`.

**رقم تليفون البايع ظاهر لأي حد** بيقرا الجدول. لو ده يقلقك، اعمل view عامة من غير عمود التليفون، وخلي الرقم يتجاب بـ RPC بعد ما المشتري يعمل طلب فعلي.

**السلة والطلبات لسه محليين.** الطلب نفسه مبيتسجلش في قاعدة البيانات — محتاج جدول `orders` وربط صفحة الدفع بيه، وده الخطوة الطبيعية الجاية.

**حد إرسال الـ OTP** مضبوط من لوحة تحكم Supabase مش من الكود. اضبطه على ٥ رسايل للرقم في الساعة على الأكتر، وإلا حد ممكن يستهلك رصيدك.
